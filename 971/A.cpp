#include <bits/stdc++.h>

using namespace std;

int main () {
    int cases;
    cin >> cases;
    for (int i = 0; i < cases; i++) {
        int first, second;
        cin >> first >> second;
        cout << second - first << endl;
    }
    return 0;
}