#include <bits/stdc++.h>

using namespace std;

#define int long long

int zero = 0;

int32_t main () {
    int cases, n, k, q;
    cin >> cases;
    while (cases--) {
        cin >> n >> k >> q;
    }
    return 0;
}
